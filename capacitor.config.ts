import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.dacongame.gamefront",
  appName: "Dacon Game",
  webDir: "dist",
  android: {
    backgroundColor: "#050309",
  },
};

export default config;
